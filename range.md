# Range(). Последовательность цисел.

Создает последовательность чисел в заданном диапазоне.

```code
  list = range(0,6)
  
  # чтобы превратить в массив, надо обернуть в функцию list()
  list = list(range(0,6))
  
  
```
